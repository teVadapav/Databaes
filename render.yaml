services:
  - type: web
    name: kisan-distress-advisory
    runtime: python
    region: oregon
    plan: free
    rootDir: coding
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server.main:app --host 0.0.0.0 --port $PORT
